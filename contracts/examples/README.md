# Contract examples

Thêm JSON request, success response và error response đã được team thống nhất vào đây.

Example payload phải hợp lệ với OpenAPI/runtime schema và không chứa secret hoặc dữ liệu người dùng thật.

