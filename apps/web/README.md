# Web client

React/Vite client nếu competition brief cần trải nghiệm web.

Chưa scaffold cho đến khi `docs/SOLUTION_SCOPE.md` chốt primary client và golden path.

Web chỉ gọi public Express API. Không đặt model key, database credential hoặc private service URL trong `VITE_*`.

